/**
 * DebugErrorThrower
 *
 * Mantido apenas como compatibilidade para imports antigos.
 * Não deve lançar erro em runtime, pois isso derruba a tela do app.
 */
export const DebugErrorThrower = () => {
  return null;
};
